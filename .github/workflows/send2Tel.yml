name: Send File to Telegram

on:
  push:
    paths:
      - 'live.txt'

jobs:
  sender:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: send default message
        uses: .
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          document: ${{ github.workspace }}/live.txt
